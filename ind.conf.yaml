# Representative inductive configuration for reviewer-side reproducibility.
# Dataset-specific tuning details and sweep scripts are intentionally omitted.
global:
  num_layers: 2
  hidden_dim: 128
  learning_rate: 0.01
  weight_decay: 0.0005
  dropout_s: 0.5
  dropout_t: 0.5
  tau: 4.0
  lamb: 0.5
  max_epoch: 1500
  use_ppr: 0
  use_tc_ppr: 1
  ppr_mode: concat
  ppr_alpha: 0.2
  ppr_k: 10
  tc_beta: 0.2
  tc_alpha: 0.2
  tc_K: 10
  tc_ind_gamma: 2.0
  tc_ind_smin: 0.2
  tc_ind_clip: 3.0
  tc_ind_smax: 0.05
  lambda_mode: pcgrad
  use_meta: 0
  ablation_mode: 0

cora:
  MLP:
    learning_rate: 0.01
    dropout_t: 0.5

citeseer:
  MLP:
    learning_rate: 0.01
    dropout_t: 0.5

pubmed:
  MLP:
    learning_rate: 0.01
    dropout_t: 0.6

chameleon:
  MLP:
    learning_rate: 0.01
    dropout_t: 0.6

romanempire:
  MLP:
    learning_rate: 0.01
    dropout_t: 0.6

arating:
  MLP:
    learning_rate: 0.01
    dropout_t: 0.6
